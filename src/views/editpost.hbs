<br>
<br>
<div class="container  bg-secondary jumbotron">
    <div class="col">
        <div class="container jumbotron">

            <form action="/saveChanges" method="post" class="form-group">
                <label for="title">Title:</label>
                <input type=" text" name="title" value="{{blog.title}}" class="form-control">

                <label for="title">Descreption:</label>
                <textarea name="desc" rows="5" class="form-control">{{blog.description}}
            </textarea>
                <br>
                <button type="submit" name="id" value="{{blog.id}}" class="btn btn-primary">
                    Save changes
                </button>
                <a class="btn btn-danger" href="/posts">Cancel</a>
            </form>
        </div>
    </div>
</div>